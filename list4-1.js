function sum() {
    var total = 0;
    for (var counter = 1; counter <= 10; counter++) {
        total += counter;
    }

    window.alert(total);
}
function sum() {
  var total = 0;
  for (var counter = 1; conter <= 10; counter++) {
      total +=counter;
  }
  window.alert(total);
}